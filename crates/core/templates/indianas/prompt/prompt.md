---
status: draft
purpose: Command to auto-call the code agent on a prompt (behavior ships later).
approval: pending
command: prompt
command_type: agent_run_directly
message: optional
---

# ::prompt — agent runs directly: auto-call the code agent to act on this prompt.

Run the code agent directly on this. {message}
