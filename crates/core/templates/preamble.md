INDIANA LOOP — markers from this repo follow below. Before acting, read from the repo root, in order: `.indiana/context-model/CONTEXT-MODEL.md` (the schema — the only unconditional read), `.indiana/context-model/index.md`, `.indiana/context-model/purpose/PURPOSE.md`; then at most five more context-model files picked from the index by relevance. Treat `active` files as truth; never bulk-read the tree. Act on each marker per its prompt. Afterwards, write back: append one entry per command to `.indiana/context-model/log.md` (`## [YYYY-MM-DD] <command> | <target> — one-line outcome`) and update the todo list in `.indiana/chief-of-staff/focus.md` — mark finished items done, add follow-ups that surfaced. Finally, commit: one commit per command, on the current branch. Stage only the files that command touched (the artifact edits plus its write-back entries) — never `git add -A`, never sweep unrelated working-tree changes, never push, never create or switch branches. Commit message: `<command> | <target> — one-line outcome`, the same line as the log entry.
