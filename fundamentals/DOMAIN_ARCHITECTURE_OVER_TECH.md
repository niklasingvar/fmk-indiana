---
status: draft
purpose: The tree is shaped by what the project is about, never by the technology it happens to use.
approval: pending
---

# DOMAIN ARCHITECTURE > TECH

## Definition
- The tree is shaped by the domain — what the project is about — never by the technology it happens to use.

## Rules
- Tech is replaceable and conforms to the domain model; the tree is never reshaped for a framework.
- Paths carry domain words; the path decides the concern.

## Test
- Swap the stack and no folder in the tree is renamed.

## Incorporation
- This repo: the concern map in [MENTAL_MODEL.md](../MENTAL_MODEL.md) — the path decides the concern.
- System prompt: the context-model seed's folder architecture maps folders to domain layers, not to tech.
